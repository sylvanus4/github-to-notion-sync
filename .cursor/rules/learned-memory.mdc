---
description: "Learned user preferences and workspace facts extracted from session transcripts. Updated by the continual-learning hook. This is the SOLE memory target — AGENTS.md is a static redirect."
alwaysApply: true
---

# Learned Memory

## Learned User Preferences

- Always respond in Korean; all generated documents (DOCX, PPTX, markdown reviews) must be entirely in Korean — English is allowed only for proper nouns and technical terms with no standard Korean translation
- Plans and skill definitions (SKILL.md) should be written in English when explicitly requested ("영어로 계획해줘")
- Use clear, assertive wording in docs; include step-by-step calculation formulas with explicit variable substitution, assumption sources, and "why this value" justifications — bare tables without derivation context are insufficient
- Follow professional, concise, technically precise doc style (reference: `01-sellable-services-catalog.md`); no ASCII box-drawing characters — use tables and hierarchical bullets for PDF/Notion compatibility
- Documents must be self-contained — no cross-references like "see document X"; each doc must stand alone when delivered externally
- TDD documents must clearly separate as-is from to-be; after creating cost/pricing documents, always verify calculation accuracy and cross-reference consistency
- Use "Slurm-on-K8s (Slinky)" terminology; clearly separate deployment modes (standalone k0s vs Thaki Suite); RACI framing should emphasize ThakiCloud doing more (investment-positive)
- Content must be substantive and detailed, never skeleton or template-only output; attribute data to source (author, date, repositories)
- Slack notifications go to `#효정-할일` channel (ID: `C0AA8NT4T8T`) — never send to DM; use threaded replies (main summary first, then individual items in thread)
- After creating or modifying a skill, always test it end-to-end and fix issues before declaring done; also create the corresponding Cursor command if one doesn't exist
- When Notion API cannot upload binary files (DOCX/PPTX), adapt content as structured sub-pages instead; Notion API does not support pipe tables — use bulleted lists or HTML table blocks
- Subagent-generated files must be verified after completion — missing output files are a pipeline failure
- For stakeholder presentations, prioritize practical demo scenarios and actual workflow examples over abstract feature descriptions; use multi-role PM critique as the default review standard for presentation materials; when enterprise audiences object "AI can't fully implement large tasks," counter with: Ralph Loop (iterative self-check against GitHub issue/PR requirements), planning ping-pong until output matches intent, multi-role review gates — the manager impact point for external demos is GitHub Issues/PR/CI → sprint summary → Notion (not general AI usage)

## Learned Workspace Facts

- ThakiCloud AI Platform (ai-platform-webui): K8s-based AI/ML platform with GPU orchestration, inference, fine-tuning; Tech stack: Go (Fiber) backend, React 19 frontend, PostgreSQL, NATS JetStream, Valkey, Keycloak, ArgoCD, Kueue, GPU Operator, Traefik
- Monitoring: VictoriaMetrics / VictoriaLogs; Lightweight K8s: k0s for standalone deployment; HPC: Slurm-on-K8s (SchedMD Slinky); GPU: H100/B200 tested
- Doc structure: TDD under `docs/technical-due-diligence/`, VC pitch under `docs/vc-pitch/`, KSA proposal under `docs/ksa-proposal/`, platform overview under `docs/platform-overview/`, financial model under `docs/financial-model/`, KPIs under `docs/kpi/`, presentations under `docs/presentations/` (with `role-use-cases/` and `workflow-automation-showcase.md`), skill guides under `docs/skill-guides/`; generated presentation assets under `output/presentations/<project-slug>/` with numbered `XX-topic.md` files; release plans and operational reviews under `output/plans/` with naming pattern `{topic}-{YYYY-MM-DD}.md`; Korean translations use `.ko.md` suffix
- Repos: ThakiCloud/tkai-deploy, ThakiCloud/ai-platform-webui, ThakiCloud/tkai-agents; EOD managed repos: github-to-notion-sync, ai-template, ai-model-event-stock-analytics, research
- Security: Thaki Suite (IAM, ABAC, STS, VXLAN) vs AI Platform (RBAC, Keycloak JWT, namespace isolation); CP communication via NATS Pub/Sub
- `paper-review` skill: 8-phase pipeline (Ingest → Review → PM Analysis → DOCX → PPTX → NLM Slides → Notion → Slack); `related-papers-scout`: 5 hot papers from top institutions; outputs: `outputs/papers/{id}-{perspective}-{DATE}.md`, `.docx`, `outputs/presentations/{id}-presentation-{DATE}.pptx`; Notion parent: `3209eddc34e6801b8921f55d85153730`; Paper/scout Slack: `#deep-research` (`C0A6X68LTN1`)
- B2B = air-gapped local inference (Thaki distilled models only, no external API egress); B2C = external API enabled (BYOK routing); Thaki model family (Thaki-8B/14B/32B, distilled from Kimi K2.5 teacher via Qwen3 base) — always use "Thaki-*" names in docs, never upstream model names (Qwen)
- Notion: no pipe tables (use bulleted lists or HTML); Node.js DOCX/PPTX scripts need `NODE_PATH=$(npm root -g)`; NotebookLM MCP requires absolute paths; Slack file upload: 3-step flow (`getUploadURLExternal` → POST file → `completeUploadExternal`); Google Workspace CLI: `gws` (alias for @googleworkspace/cli), not `gwcli`
- The git-push-pull `tmp` branch rule applies ONLY to `ai-platform-webui`; Phase 3 subagents must use the Write tool to persist their markdown outputs; `release-ship`, `commit-to-issue`, and `github-workflow-automation` use local reference docs only (no Notion runtime fetch); ai-platform-webui pre-commit hook enforces Conventional Commits (feat:, fix:, docs:, chore:, refactor:, style:, test:, perf:, ci:, build:, revert:) — `enhance` is rejected and hook takes precedence over CONTRIBUTING.md `[feat]` format; `gh project item-edit` fails when `GITHUB_TOKEN` env var is set without `read:org` scope — workaround: use `gh api graphql` mutations directly for project field updates
- `meeting-digest` output: `output/meetings/YYYY-MM-DD/<meeting-slug>/` with per-meeting subfolders (summary.md, action-items.md, raw/); supports DOCX + Google Drive upload + Slack as post-processing; Notion meeting upload to parent page `3239eddc34e680e8a7a5d5b5eac18b38`; `md-to-notion` is a separate one-shot markdown-to-Notion publisher (distinct from `notion-docs-sync`)
- `sod-ship`: Start-of-day bidirectional git sync (commit → push → pull → verify) for all 5 managed projects; counterpart to `eod-ship`; reuses eod-ship project-registry.md; ai-platform-webui uses tmp branch for both push and pull
- `office-template-enforcer`: 3-skill system (orchestrator + `ppt-template-engine` + `docx-template-engine`) for company-compliant DOCX/PPTX; uses fixed template assets + JSON intermediate spec (LLM generates content only, never styles) + validation scripts; templates stored in each skill's `assets/templates/`
- MiroFish는 L5 분석 도구 — L1~L4 스킬(daily-stock-check → trading-scenario-analyzer → alphaear-deepear-lite → paper-review)이 부족할 때만 에스컬레이션; paper-review 후속 검증, 복잡 시나리오 비선형 상호작용, 위기/여론 시뮬레이션, 주요 투자 결정 전 예행연습에 적합; 일상적 분석에 사용 금지
