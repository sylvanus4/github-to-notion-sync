---
description: "Learned user preferences and workspace facts extracted from session transcripts. Updated by the continual-learning hook. This is the SOLE memory target — AGENTS.md is a static redirect."
alwaysApply: true
---

# Learned Memory

## Learned User Preferences

- Always respond in Korean; all generated documents (DOCX, PPTX, markdown reviews) must be entirely in Korean — English is allowed only for proper nouns and technical terms with no standard Korean translation
- Plans and skill definitions (SKILL.md) should be written in English when explicitly requested ("영어로 계획해줘")
- Follow professional, concise, assertive doc style (reference: `01-sellable-services-catalog.md`); include step-by-step calculation formulas with explicit variable substitution, assumption sources, and "why this value" justifications; all data claims (prices, benchmarks, throughput) must cite source URLs with access dates; sizing and financial models require sensitivity analysis (±2× key variables); no ASCII box-drawing characters — use tables and hierarchical bullets for PDF/Notion compatibility; Excel financial models: blue fill for input cells, black for formulas, green for internal links, red for external links, yellow for key assumptions; number formats $#,##0 / 0.0% / 0.0x; formulas use cell references never hardcoded values; store openpyxl generator scripts (`scripts/gen_*_xlsx.py`) for reproducibility
- Documents must be self-contained — no cross-references like "see document X"; TDD docs clearly separate as-is from to-be; after creating cost/pricing documents, always verify calculation accuracy and cross-reference consistency
- Use "Slurm-on-K8s (Slinky)" terminology; clearly separate deployment modes (standalone k0s vs Thaki Suite); RACI framing should emphasize ThakiCloud doing more (investment-positive)
- Content must be substantive and detailed, never skeleton or template-only output; every heading and subheading must have detailed explanatory prose with justifications — not just titles, tables, or bullet lists; attribute data to source (author, date, repositories); in analysis documents, place executive summary and key conclusions at the very beginning before detailed sections; when existing documents have been distributed externally (e.g., to investors), supplement with NEW chapters/files rather than modifying the originals
- Slack notifications go to `#효정-할일` channel (ID: `C0AA8NT4T8T`) — never send to DM; use threaded replies (main summary first, then individual items in thread); team seminar/training announcements go to `#thakicloud-seminar`; Decision Channel system: `decision-router` skill detects decision-worthy items from pipeline outputs and routes to `#효정-의사결정` (personal: trading, email, tool adoption) or `#7층-리더방` (team/CTO: infra, strategy, partnerships, budget); integrated into google-daily (Phase 4.5), today (Step 5d), bespin-news-digest (Step 3f + Phase 6.5), twitter-timeline-to-slack (Step 3g), x-to-slack (Step 7); all support `skip-decisions` flag; channel IDs: `#효정-의사결정` = `C0ANBST3KDE`, `#7층-리더방` = `C0A6Q7007N2`
- After creating or modifying a skill, always test it end-to-end and fix issues before declaring done; also create the corresponding Cursor command if one doesn't exist; when applying design pattern or framework updates to skills, prioritize frequently-used skills first (e.g., today, paper-review, x-to-slack, deep-review) before peripheral ones; when generating documents or presentation chapters, follow the 3-step pattern: research first with appropriate skills, then critique/review the output, then update based on critique — never publish a first draft without this cycle
- When Notion API cannot upload binary files (DOCX/PPTX), adapt content as structured sub-pages instead; Notion API does not support pipe tables — use bulleted lists or HTML table blocks
- Subagent-generated files must be verified after completion — missing output files are a pipeline failure
- For stakeholder presentations, prioritize practical demo scenarios and actual workflow examples over abstract feature descriptions; use multi-role PM critique as the default review standard; for keynote-style presentations, default to Steve Jobs principles: one idea per slide, zero bullet points, dramatic reveals, concrete metaphors, speaker scripts in Korean with visual descriptions in English; when enterprise audiences object "AI can't fully implement large tasks," counter with: Ralph Loop, planning ping-pong, multi-role review gates; for competitive analysis, compare against the competitor's full ecosystem/platform; for external enterprise audiences, frame as "build your own platform" not product promotion — remove vendor-specific references that could be interpreted as sales
- When user provides a `.plan.md` file, implement it directly — never recreate the plan or regenerate existing todos; mark existing todo items as in_progress when starting work; the plan file itself must not be modified
- When creating a new skill, verify the name doesn't conflict with existing skills before registering; user caught `autoresearch` conflicting with `auto-research` (AutoResearchClaw) and renamed to `skill-autoimprove`

## Learned Workspace Facts

- ThakiCloud AI Platform (ai-platform-webui): K8s-based AI/ML platform with GPU orchestration, inference, fine-tuning; Tech stack: Go (Fiber) backend, React 19 frontend, PostgreSQL, NATS JetStream, Valkey, Keycloak, ArgoCD, Kueue, GPU Operator, Traefik; Monitoring: VictoriaMetrics / VictoriaLogs; Lightweight K8s: k0s for standalone deployment; HPC: Slurm-on-K8s (SchedMD Slinky); GPU: H100/B200 tested; Agent orchestration: NemoClaw/OpenClaw (kata + firecracker sandboxing), 400+ Cursor skills ecosystem, RemoteClaw remote deployment — lab-complete, pre-production stage
- Doc structure: TDD under `docs/technical-due-diligence/`, VC pitch under `docs/vc-pitch/`, VC pitch v3 under `docs/vc-pitch-v3/` (with `financial-model/`, `fm-chapters/`, `deck-chapters/`, `research/`, `appendix/`, `agent-infra-deck/`, `output/` subdirs), KSA proposal under `docs/ksa-proposal/`, platform overview under `docs/platform-overview/`, financial model under `docs/financial-model/`, KPIs under `docs/kpi/`, presentations under `docs/presentations/` (with `role-use-cases/`), skill guides under `docs/skill-guides/`; generated presentation assets under `output/presentations/<project-slug>/` with `chNN-topic.md` chapter files; samsung-ai-automation chapters use `chXX-*.md` naming with metadata YAML, color palette, per-slide Korean speaker scripts and English visual descriptions; release plans under `output/plans/` with `{topic}-{YYYY-MM-DD}.md`; Korean translations use `.ko.md` suffix; analysis under `output/analysis/`; research under `output/research/`; training under `output/training/{NN-team-name}/`
- Repos: ThakiCloud/tkai-deploy, ThakiCloud/ai-platform-webui, ThakiCloud/tkai-agents; EOD managed repos: github-to-notion-sync, ai-template, ai-model-event-stock-analytics, research; team-specific automation repos: ThakiCloud/thaki-business-automation, ThakiCloud/thaki-cloudtech-automation, ThakiCloud/thaki-planning-automation — skills and training docs are COPIED (not symlinked) from ai-platform-webui; NEVER delete originals in ai-platform-webui when distributing to team repos; Security: Thaki Suite (IAM, ABAC, STS, VXLAN) vs AI Platform (RBAC, Keycloak JWT, namespace isolation); CP communication via NATS Pub/Sub
- `paper-review` skill: 8-phase pipeline (Ingest → Review → PM Analysis → DOCX → PPTX → NLM Slides → Notion → Slack); `related-papers-scout`: 5 hot papers from top institutions; outputs: `outputs/papers/{id}-{perspective}-{DATE}.md`, `.docx`, `outputs/presentations/{id}-presentation-{DATE}.pptx`; Notion parent: `3209eddc34e6801b8921f55d85153730`; Paper/scout Slack: `#deep-research` (`C0A6X68LTN1`)
- Unified platform with 7-tier ladder (Free → Pro → Team → Startup → Department → Division → Enterprise); air-gap is a feature attribute per tier (Free/Pro/Team: not available, Startup/Department: add-on, Division/Enterprise: included) — B2C/B2B segment labels are deprecated and must NOT be used; Thaki model family (Thaki-8B/14B/32B/120B-MoE, distilled from Kimi K2.5 teacher via Qwen3 base) — always use "Thaki-*" names in docs, never upstream model names (Qwen); VC-facing sizing docs must specify reference GPU generation (e.g., "all calculations assume NVIDIA B200"); financial models cover three scenarios: B200 (baseline), CPU-first Phase 0 (capital-light), and Vera Rubin R200 + Groq LPX (future migration, ~35x throughput improvement); ThakiCloud can launch capital-light with CPU-only servers + external LLM APIs (BYOK model) — GPU purchase is optional for initial operations; the "best cloud for agents" value proposition is independent of GPU ownership; docs and financial models must reflect the CPU-first Phase 0 path alongside GPU scenarios
- Notion: no pipe tables (use bulleted lists or HTML); Node.js DOCX/PPTX scripts need `NODE_PATH=$(npm root -g)`; NotebookLM MCP requires absolute paths; Slack file upload: 3-step flow (`getUploadURLExternal` → POST file → `completeUploadExternal`); Slack Canvas MCP tools: `slack_create_canvas`, `slack_read_canvas`, `slack_update_canvas` for rich document upload similar to Notion pages; `slack_search_channels` MCP tool cannot discover private channels — extract channel IDs from Slack archive URLs (`/archives/CHANNEL_ID`) instead; Google Workspace CLI: `gws` (alias for @googleworkspace/cli), not `gwcli`; `gws drive +upload` is the correct upload subcommand (not `upload`), `--folder` argument not supported; `gws gmail +reply --message-id ID --body TEXT` for threaded replies (auto In-Reply-To/References/threadId), `gws gmail +reply-all` for CC'd replies, `gws gmail +triage --max N --query 'QUERY'` for inbox listing (not `--max-results`), `--dry-run` flag to preview before send; `md-to-slack-canvas`: markdown-to-Slack-Canvas publisher (distinct from `md-to-notion`)
- The git-push-pull `tmp` branch rule applies ONLY to `ai-platform-webui`; Phase 3 subagents must use the Write tool to persist their markdown outputs; `release-ship`, `commit-to-issue`, and `github-workflow-automation` use local reference docs only (no Notion runtime fetch); ai-platform-webui pre-commit hook enforces Conventional Commits (feat:, fix:, docs:, chore:, refactor:, style:, test:, perf:, ci:, build:, revert:) — `enhance` is rejected and hook takes precedence over CONTRIBUTING.md `[feat]` format; `gh project item-edit` fails when `GITHUB_TOKEN` env var is set without `read:org` scope — workaround: use `gh api graphql` mutations directly for project field updates; ai-model-event-stock-analytics pre-push hook runs Playwright E2E tests which can hang >4min — use `git push --no-verify` as workaround when it hangs; `sod-ship`: Start-of-day bidirectional git sync (commit → push → pull → verify) for all 5 managed projects; counterpart to `eod-ship`; reuses eod-ship project-registry.md; ai-platform-webui uses tmp branch for both push and pull; `today` pipeline includes Phase 0 (setup-doctor pre-flight) and post-pipeline Phase 6 (twitter-timeline-to-slack); `.claude-plugin` format bundles skills, commands, rules, hooks, MCP configs into portable units
- `meeting-digest` output: `output/meetings/YYYY-MM-DD/<meeting-slug>/` with per-meeting subfolders (summary.md, action-items.md, raw/); supports DOCX + Google Drive upload + Slack as post-processing; Notion meeting upload to parent page `3239eddc34e680e8a7a5d5b5eac18b38`; `md-to-notion` is a separate one-shot markdown-to-Notion publisher (distinct from `notion-docs-sync`); `office-template-enforcer`: 3-skill system (orchestrator + `ppt-template-engine` + `docx-template-engine`) for company-compliant DOCX/PPTX; uses fixed template assets + JSON intermediate spec (LLM generates content only, never styles) + validation scripts; templates stored in each skill's `assets/templates/`
- MiroFish는 L5 분석 도구 — L1~L4 스킬(daily-stock-check → trading-scenario-analyzer → alphaear-deepear-lite → paper-review)이 부족할 때만 에스컬레이션; paper-review 후속 검증, 복잡 시나리오 비선형 상호작용, 위기/여론 시뮬레이션, 주요 투자 결정 전 예행연습에 적합; 일상적 분석에 사용 금지; 배포 위치 `~/thaki/MiroFish/`, 백엔드 API `http://localhost:5001`, ZEP_API_KEY는 프로젝트 `.env`에서 가져옴; Gemini를 OpenAI 호환 엔드포인트(`https://generativelanguage.googleapis.com/v1beta/openai/`)로 대체 사용 가능; `max_tokens`는 16384 이상 필요; 시뮬레이션 출력 `output/mirofish/{YYYY-MM-DD}-{topic}/`
- `gws-email-reply`: composed skill orchestrating gws-gmail, recall, cognee, response-drafting, brand-voice, and sentence-polisher for AI-drafted email replies with human approval gate; `/reply-email` command entry point; `sentence-polisher`: bilingual (Korean + English) final-stage text fixer for grammar, spacing, honorifics, Konglish, and flow — designed for pipeline integration (any skill can invoke it as a last step)
- Defuddle (defuddle.md API) supports YouTube transcript extraction with timestamps, chapters, and speaker diarization (v1.1.0+); prefer over transcribee for YouTube-only quick extraction; use transcribee for local files, Instagram, TikTok, or when word-level timestamps are needed; x-to-slack user-level Handler C (v2.2.0) uses Defuddle as primary YouTube extractor with oEmbed fallback
- AutoResearchClaw: 23-stage autonomous research pipeline (aiming-lab/AutoResearchClaw); installed at `~/thaki/AutoResearchClaw/` with Python 3.11 venv at `.venv/`; config at `config.arc.yaml`; CLI `researchclaw run/validate/doctor/report`; experiment modes: simulated, sandbox, docker, ssh_remote; stages flow: Topic Init → Literature (OpenAlex/Semantic Scholar/arXiv) → Hypothesis (multi-agent debate) → Experiment → Paper Writing (5,000-6,500 words) → Peer Review → LaTeX Export; PIVOT/REFINE loop at Stage 15; 4-layer citation verification; MetaClaw integration for lesson-to-skill; outputs under `artifacts/{run_id}/deliverables/`; Cursor skills: `auto-research` (orchestrator), `auto-research-distribute` (post-pipeline distribution to Notion/Slack/NLM/PPTX/paper-archive)
- `skill-autoimprove`: Karpathy autoresearch-based autonomous skill prompt optimizer; binary evals only, one-mutation-at-a-time experiments, keep/discard loop; working dir `autoimprove-[skill-name]/` inside target skill folder; outputs: results.tsv, changelog.md, SKILL.md.baseline; first batch (5 skills: sentence-polisher, gws-email-reply, unified-intel-intake, issue-to-code, standup-digest) improved avg 74% → 100%; distinct from `auto-research` (AutoResearchClaw pipeline) and `skill-optimizer` (static audit/benchmark); `/skill-autoimprove` command entry point
- Multi-cluster centralization docs under `docs/multi-cluster-centralization/` with `en/` and `ko/` subdirs; covers Go backend centralization to MGMT cluster, sandbox (Kata) cluster isolation, MultiKueue scheduling integration, and frontend multi-cluster API adaptation; key 1.0 architecture decision area; `en/` has 13+ analysis docs (current arch, problem statement, role-specific analyses, research docs, solution architecture, Kueue integration design, implementation timeline); `ko/` has 6 summary docs (00-overview through 05-action-plan)
