# skill-autoimprove — mirofish-opinion-sim

## Experiment 0 (baseline)

- Major EVAL 4 failure: documented `simulation/create` fields inconsistent with backend. EVAL 1–3 thin for AI regulation / crisis scenarios.

## Experiment 1

- Rewrote workflow to match canonical MiroFish graph + simulation + report API order.
- Added **Quality Contract** with seed structure, defaults, opinion-specific report bullets, and API enumeration.
- Added `Content-Type: application/json` to `report/chat` example; softened unverified `platform_type` claim.
- **Files changed:** `SKILL.md`
