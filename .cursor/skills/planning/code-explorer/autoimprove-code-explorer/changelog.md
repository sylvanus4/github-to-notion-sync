# code-explorer — skill-autoimprove changelog

## Cycle: 2025-03-24

### Baseline

- Source: `SKILL.md` copied to `SKILL.md.baseline` (metadata version `1.1.0`).

### Mutations applied (targeted)

1. **Translation quick-reference (E1)** — Extended Step 3 table with rows for `callback`/`props`/state mutation/`ref`/`key`/`memo`/`try`·`catch` and Korean planning equivalents.
2. **Anti-patterns (E1)** — New subsection forbidding variable/hook-led explanations and unsolicited code dumps; require role-based Korean phrasing.
3. **Pre-output translation checklist + mandatory citations (E1, E2)** — Before the answer template: checklist for planning language and identifier avoidance; **every** code-backed claim must include `path:line` or line-range citation, with explicit fallback when lines unknown.
4. **Template + examples** — `Code locations` bullet format now shows `` `path:line` ``; illustrative answers emphasize Korean planning prose and file:line; **E2** evolution row tightened to match.

### Version

- `metadata.version`: `1.1.0` → `1.2.0`

### Line budget

- `SKILL.md` line count after edit: **201** (limit 500).

### Alignment notes

- Repo `eval-criteria.md` (EVAL 1–4) overlaps with Evolution **E1–E4**; this cycle primarily strengthens **E1** and **E2** per skill-autoimprove instructions. **E3/E4** unchanged in structure; boundary text already satisfied **E4**.
