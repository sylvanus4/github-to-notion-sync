# Leaderboard Report Templates

## Markdown Report

```markdown
# AI Leaderboard Tracker — {DATE}

## Summary
{1-2 sentences: leaderboards checked, biggest mover, new entries count}

## Open LLM Leaderboard

### Top 10
| Rank | Model | Avg Score | Params | Change |
|------|-------|-----------|--------|--------|
| {rank} | {model} | {score} | {params} | {status} |

### Notable Movers
- **{model}**: #{prev} -> #{curr} (+{delta}) — {reason}

### New Entries
- **{model}** debuted at #{rank} with score {score}

## Chatbot Arena

### Top 10
| Rank | Model | Elo | Votes | Change |
|------|-------|-----|-------|--------|
| {rank} | {model} | {elo} | {votes} | {status} |

## Video Generation

### Top 10
| Rank | Model | Score | Change |
|------|-------|-------|--------|
| {rank} | {model} | {score} | {status} |

## Cross-Leaderboard Insights
{Models performing well across multiple benchmarks}

---
*Generated by hf-leaderboard-tracker | {DATE}*
```

## Slack Main Message

```
:trophy: *AI Leaderboard Tracker — {DATE}*

*{LEADERBOARD_COUNT}* leaderboards scanned

:arrow_up: *Biggest movers:*
{MODEL_1}: #{OLD} -> #{NEW} (+{DELTA}) on {LEADERBOARD}
{MODEL_2}: #{OLD} -> #{NEW} (+{DELTA}) on {LEADERBOARD}

:new: *New entries:* {NEW_COUNT} models debuted today
```

## Slack Thread Reply — Per Leaderboard

```
:bar_chart: *{LEADERBOARD_NAME}*

*Top 5:*
1. `{model}` — {score} {STATUS_ICON}
2. `{model}` — {score} {STATUS_ICON}
3. `{model}` — {score} {STATUS_ICON}
4. `{model}` — {score} {STATUS_ICON}
5. `{model}` — {score} {STATUS_ICON}

:arrow_up: *Movers:*
{model}: +{delta} ranks
{model}: -{delta} ranks

:new: *New:*
{model}: debuted at #{rank}
```

## Status Icons

- `:arrow_up:` — rank improved
- `:arrow_down:` — rank declined
- `:arrow_right:` — stable (within ±1)
- `:new:` — new entry
- `:fire:` — top 3 and improving

## No-Changes Message

Do NOT post to Slack. Log locally:
```
[{DATE}] No significant leaderboard changes detected. Skipping Slack distribution.
```
